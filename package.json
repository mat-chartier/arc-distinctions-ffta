{
    "scripts": {
        "frontend:start": "cd front && npm start",
        "frontend:deploy": "npm run --prefix front deploy"
    },
    "devDependencies": {
        "firebase-admin": "^13.6.1",
        "firebase-tools": "^14.27.0"
    }
}
